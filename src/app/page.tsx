export default function HomePage() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui" }}>
      <h1>Job Aggregator</h1>
      <p>
        Starter scaffold is installed. Next: implement the first source adapter
        and the jobs inbox.
      </p>
    </main>
  );
}
